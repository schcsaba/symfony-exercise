<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home_index') }}">DevJobs</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a {% if route_name == "profile_index" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('profile_index') }}">My account</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a {% if route_name == "app_login" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('app_login') }}">Login</a>
                    </li>
                    <li class="nav-item">
                        <a {% if route_name == "register_index" %} class="nav-link active" {% else %} class="nav-link" {% endif %} href="{{ path('register_index') }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>