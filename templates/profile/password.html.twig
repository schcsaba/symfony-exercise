{% extends 'base.html.twig' %}

{% block title %}{{ 'account.password.change'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <h1>{{ 'account.password.change'|trans }}</h1>
            {% if notification %}
                <div class="alert alert-info">{{ notification }}</div>
            {% endif %}
            {{ form(form) }}
        </div>
    </div>
{% endblock %}